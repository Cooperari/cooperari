== 0.2.1. final ==

Features left to implement:
- Pretty print for cjunit 
- cjunit / csetup chaining ... load agent programatically?
- cjunit -p switch / ensure CJUnitRunner runs preemptively as well
- Revise Coverage code
- CSystem.runPreemptively() 
- CSystem.executeSession()
- Windows scripts


Misc. implementation details
 -- Merge CRuntime / CSession ?
 -- Get rid of Scheduler.wakeup() interaction
 -- operation arguments
 -- resource deadlocks / wait release step ?
 -- improve wait/notify tests + check tests being ignored
 -- CBlockingOperationResult used more often (sleep, join, ..., also)
 -- Inline yield points - refactoring
 -- feature package names ?
 -- serial ids for exception classes
 -- Move Noise, NonCooperativeThreadRunner etc to attic package

Future workloads:
- Examples project
- Site organization.

== 0.3 ==

Features:
- java.util.concurrent (Cyclic Barrier, phaser, etc)
- CScheduler more efficient
- java.io ? 
- java.net ? 
- randomized testing plugin
